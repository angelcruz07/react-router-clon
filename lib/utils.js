import{EVENTS}from"./const";export function navigate(href){window.history.pushState({},"",href);const navigationEvent=new Event(EVENTS.PUSHSATE);window.dispatchEvent(navigationEvent)}export const getCurrentPath=()=>window.location.pathname;